{
  "name": "2fas-vault",
  "version": "1.0.0",
  "private": true,
  "description": "2FAS Vault — production-ready 2FA management webapp (Node.js + Express + Vanilla JS) backed by Firebase Realtime Database. OTP generation is performed by a pure-JS isomorphic engine (OTPAuth 9.1.1), with no Web Crypto / SubtleCrypto dependency.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
